query MyPosts($circleId: Int!, $cursor: String) {
  myPosts(circleId: $circleId, cursor: $cursor) {
    hasMore
    data {
      id
      circleId
      createdAt
      likesCount
      hasLiked
      text
    }
  }
}
