query Circle($circleId: Int!) {
  circle(circleId: $circleId) {
    id
    name
    description
    createdAt
    totalMembers
    isAdmin
    isMember
    isPublic
    creator {
      username
    }
  }
}
